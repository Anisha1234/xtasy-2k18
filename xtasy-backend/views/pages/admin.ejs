 <!DOCTYPE html>
 <html>
   <head>
     <%- include ../partials/_head %>
   </head>
   <body>
    <h1>Admin</h1>
    <table class = "table table-hover">
      <tr>
        <th>Xtasy ID</th>
        <th>NAME</th>
        <th>EMAIL ID</th>
        <th>COLLEGE</th>
      </tr>
      <% for(var i=0; i < users.length; i++) { %>
      <tr>
        <td><%= users[i].xtasyid %></td>
       <td><%= users[i].name %></td>
       <td><%= users[i].emailid %></td>
       <td><%= users[i].college %></td>
      </tr>
      <% } %>
    </table>

  </body>
 </html>
